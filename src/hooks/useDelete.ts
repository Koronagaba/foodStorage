import { db } from '../firebase/config'
import { deleteDoc } from 'firebase/firestore'

export const useDelete = async () => {

    // const deleteHook = () => {
    //     const ref = 
    // }

    // return { deleteHook }
}